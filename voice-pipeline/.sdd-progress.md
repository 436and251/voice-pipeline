# SDD ledger — plan: docs/superpowers/plans/2026-08-31-v2proplus-framework.md

Task 1: complete — package/CLI skeleton, tests green.
Task 2: complete — v2ProPlus profile registry, tests green.
Task 3: complete — asset verification + CLI, tests green.
Task 4: complete — manifest/run-state/experiment layout, tests green.
Task 5: complete — stage graph/signature, tests green.
Task 6 slice A: complete — ZH/JA/EN symbol IDs, parity tests green.
Task 6 slice B: complete — Japanese normalization + pyopenjtalk prosody/G2P orchestration; 29 passed, 2 dependency-gated skips.

## Milestone 4 — Chinese frontend core foundation
- Added RED-first parity tests for punctuation cleanup, mixed punctuation collapse, erhua allow/deny behavior, zero-initial/final rewrites, OpenCPOP mapping and `word2ph`.
- Added the pinned 429-entry `opencpop-strict.txt` resource.
- Implemented only pure Chinese frontend primitives; G2PW/TextNormalizer/ToneSandhi orchestration remains intentionally out of scope for this slice.
- Regression: 36 passed, 2 skipped.


## Restored reviewed training baseline
- Restored from Milestone 5 after sandbox reset.
- Added v2ProPlus V2/Pro route guard: `s2_train.py`, `SynthesizerTrn`, MPD, SV, GAN=true, CFM=false, external vocoder=false, text low-LR=0.4.
- Added exact S1 upstream optimizer gate controller (`batch_idx=4,8,...`).
- Added S2 D->G AMP orchestration contract with tuple-loss extraction and one completed D+G batch counted as one step.
- Added SoVITS `06` checkpoint codec backed by a real torch archive.
- Added isolated pure ToneSandhi parity slice; heavy runtime rules remain explicitly deferred.
- Regression baseline: 50 passed, 2 dependency-gated skips.

## S1 Core Minimal Vendoring + Real Model Construction
- Vendored the pinned upstream `Text2SemanticDecoder` and its seven-file minimal
  transformer closure with package-relative imports only.
- Added `build_s1_model(...)` and strict official-checkpoint loading that removes
  the Lightning `model.` prefix.
- Verified the real `s1v3.ckpt` constructs a 24-layer, 512-wide model with
  semantic vocab 1025 and phoneme vocab 732.
- Optimizer, scheduler, data, trainer, ONNX, and V3/V4 code remain deferred.

## v2ProPlus S2 Core Minimal Vendoring + Real Model Construction
- Vendored the pinned upstream S2 file-level dependency closure.
- Extracted only the v2ProPlus `SynthesizerTrn`, waveform generator, posterior/
  flow, and eight-branch `MultiPeriodDiscriminator` model ranges.
- Added strict S2G/S2D official-checkpoint loaders with forced v2ProPlus routing.
- CFM, F5-TTS, V3/V4, external vocoder, trainer, data and CLI remain excluded.

## v2ProPlus Feature Extractors
- Added ffmpeg-compatible mono 32 kHz audio loading without WebUI dependencies.
- Added local-only CN-HuBERT loading and `(batch, 768, frames)` extraction.
- Adapted the minimal ERes2NetV2 `forward3()` speaker path with strict official
  checkpoint loading and `(batch, 20480)` output.
- Reused torchaudio's Kaldi fbank after confirming bit-identical output against
  the pinned upstream copy.

## Task 6 — Complete multilingual frontend

- Added a validated frontend result contract and local-only Chinese RoBERTa
  character-to-phone alignment.
- Completed Chinese TextNormalizer, full ToneSandhi, G2PW ONNX, pronunciation
  correction, erhua, and OpenCPOP. Real `重庆` polyphone regression passes without
  a runtime downloader.
- Added real pyopenjtalk Japanese and GPT-SoVITS/g2p_en English adapters. English
  uses the explicit `averaged_perceptron_tagger_eng` directory and pinned lexical
  resources without NLTK downloads.
- Added explicit ZH/JA/EN routing and local-model mixed segmentation. Only Chinese
  spans emit real BERT features; JA/EN spans emit aligned zero features.
- Final real-asset regression: 79 passed, 0 skipped. It exercised S1, S2G/S2D,
  CN-HuBERT, ERes2NetV2, Chinese RoBERTa, G2PW ONNX, pyopenjtalk, the explicit
  NLTK English tagger, and local fast-langdetect.
